using System.Windows;

namespace CommonFeature
{
    /// <summary>
    /// Code-behind for CommonFeatureWindow.
    /// Only handles window lifecycle - no business logic.
    /// </summary>
    public partial class CommonFeatureWindow : Window
    {
        public CommonFeatureWindow()
        {
            InitializeComponent();
        }
    }
}
