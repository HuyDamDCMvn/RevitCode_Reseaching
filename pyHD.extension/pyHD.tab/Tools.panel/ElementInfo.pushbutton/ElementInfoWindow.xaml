<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Element Information" 
        Height="600" 
        Width="1100"
        MinHeight="450"
        MinWidth="850"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResizeWithGrip"
        ShowInTaskbar="True"
        Background="#FAFAFA"
        FontFamily="Segoe UI"
        FontSize="13">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Padding="20,16" Background="#3F51B5">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <TextBlock Text="Element Information" 
                               FontSize="20" 
                               FontWeight="SemiBold"
                               Foreground="White"/>
                    <TextBlock x:Name="CountText"
                               Text="0 elements" 
                               FontSize="12" 
                               Foreground="#BBDEFB"
                               Margin="0,4,0,0"/>
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button x:Name="AddParamButton"
                            Padding="12,8"
                            Margin="0,0,8,0"
                            Background="#4CAF50"
                            Foreground="White"
                            BorderThickness="0"
                            ToolTip="Add parameter columns from selected elements"
                            Cursor="Hand">
                        <TextBlock Text="Add Param"/>
                    </Button>
                    <Button x:Name="UpdateButton"
                            Padding="12,8"
                            Margin="0,0,8,0"
                            Background="#FF9800"
                            Foreground="White"
                            BorderThickness="0"
                            ToolTip="Update modified values to Revit model"
                            Cursor="Hand"
                            IsEnabled="False">
                        <TextBlock Text="Update Value"/>
                    </Button>
                    <Button x:Name="ClearFilterButton"
                            Padding="12,8"
                            Margin="0,0,8,0"
                            Background="#5C6BC0"
                            Foreground="White"
                            BorderThickness="0"
                            ToolTip="Clear all filters"
                            Cursor="Hand">
                        <TextBlock Text="Clear Filters"/>
                    </Button>
                    <Button x:Name="ExportButton"
                            Padding="12,8"
                            Margin="0,0,8,0"
                            Background="#5C6BC0"
                            Foreground="White"
                            BorderThickness="0"
                            ToolTip="Export to CSV"
                            Cursor="Hand">
                        <TextBlock Text="Export CSV"/>
                    </Button>
                    <Button x:Name="RefreshButton"
                            Padding="12,8"
                            Background="#5C6BC0"
                            Foreground="White"
                            BorderThickness="0"
                            ToolTip="Refresh data"
                            Cursor="Hand">
                        <TextBlock Text="Refresh"/>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <!-- DataGrid with horizontal scroll -->
        <DataGrid x:Name="ElementsGrid"
                  Grid.Row="1"
                  Margin="16,8,16,8"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  CanUserReorderColumns="True"
                  CanUserResizeColumns="True"
                  CanUserSortColumns="True"
                  IsReadOnly="False"
                  SelectionMode="Extended"
                  SelectionUnit="FullRow"
                  GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column"
                  BorderThickness="1"
                  BorderBrush="#E0E0E0"
                  Background="White"
                  VirtualizingPanel.IsVirtualizing="True"
                  VirtualizingPanel.VirtualizationMode="Recycling"
                  ScrollViewer.HorizontalScrollBarVisibility="Auto"
                  ScrollViewer.VerticalScrollBarVisibility="Auto"
                  ScrollViewer.CanContentScroll="True">
            
            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="#F5F5F5"/>
                    <Setter Property="Foreground" Value="#424242"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                    <Setter Property="BorderBrush" Value="#E0E0E0"/>
                    <Setter Property="BorderThickness" Value="0,0,1,1"/>
                    <Setter Property="Padding" Value="8,6"/>
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                </Style>
            </DataGrid.ColumnHeaderStyle>
            
            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="#E3F2FD"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.RowStyle>
        </DataGrid>

        <!-- Filter Popup -->
        <Popup x:Name="FilterPopup" 
               StaysOpen="False" 
               Placement="Bottom"
               AllowsTransparency="True">
            <Border Background="White"
                    BorderBrush="#BDBDBD"
                    BorderThickness="1"
                    Padding="12"
                    MinWidth="220"
                    MaxHeight="380">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="8" ShadowDepth="2" Opacity="0.25"/>
                </Border.Effect>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <!-- Search Box -->
                    <TextBox x:Name="PopupSearchBox"
                             Grid.Row="0"
                             Margin="0,0,0,8"
                             Padding="8,6"
                             FontSize="12"
                             BorderBrush="#BDBDBD"
                             BorderThickness="1"/>
                    
                    <!-- Select All / Clear All -->
                    <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,8">
                        <Button x:Name="SelectAllBtn" Content="Select All" FontSize="11" Padding="10,5" Margin="0,0,8,0"
                                Background="#E8E8E8" BorderThickness="0" Cursor="Hand"/>
                        <Button x:Name="ClearAllBtn" Content="Clear All" FontSize="11" Padding="10,5"
                                Background="#E8E8E8" BorderThickness="0" Cursor="Hand"/>
                    </StackPanel>
                    
                    <!-- CheckBox List -->
                    <ScrollViewer Grid.Row="2" MaxHeight="220" VerticalScrollBarVisibility="Auto">
                        <StackPanel x:Name="FilterItemsPanel"/>
                    </ScrollViewer>
                    
                    <!-- Apply Button -->
                    <Button x:Name="ApplyFilterBtn" Grid.Row="3" Content="Apply Filter" Margin="0,10,0,0" Padding="16,8"
                            Background="#3F51B5" Foreground="White" BorderThickness="0" Cursor="Hand"/>
                </Grid>
            </Border>
        </Popup>

        <!-- Footer -->
        <Border Grid.Row="2" 
                Background="#FAFAFA"
                BorderBrush="#E0E0E0"
                BorderThickness="0,1,0,0"
                Padding="16,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <TextBlock Grid.Column="0"
                           x:Name="StatusText"
                           Text="Ready" 
                           FontSize="12"
                           VerticalAlignment="Center"
                           Foreground="#757575"/>
                
                <TextBlock Grid.Column="1"
                           x:Name="ModifiedText"
                           Text=""
                           FontSize="12"
                           VerticalAlignment="Center"
                           Foreground="#FF9800"
                           Margin="0,0,16,0"/>
                
                <Button Grid.Column="2"
                        x:Name="CloseButton"
                        Content="Close"
                        Padding="20,8"
                        Background="#3F51B5"
                        Foreground="White"
                        BorderThickness="0"
                        Cursor="Hand"/>
            </Grid>
        </Border>
    </Grid>
</Window>
